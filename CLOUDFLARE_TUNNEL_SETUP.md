# Настройка Cloudflare Tunnel (без домена!)

## ✅ Лучшее решение, если нет домена

Cloudflare Tunnel позволяет получить HTTPS URL с валидным SSL сертификатом **без домена**!

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Установить Cloudflare Tunnel

```bash
# Скачать cloudflared
curl -L https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb

# Установить
sudo dpkg -i cloudflared.deb

# Проверить установку
cloudflared --version
```

### Шаг 2: Авторизоваться в Cloudflare

```bash
# Запустить авторизацию
cloudflared tunnel login
```

1. Откроется браузер
2. Войдите в Cloudflare (или зарегистрируйтесь бесплатно)
3. Выберите домен (или создайте новый аккаунт)
4. Авторизуйте туннель

### Шаг 3: Создать туннель

```bash
# Создать туннель
cloudflared tunnel create telegram-shop
```

Сохраните ID туннеля (будет показан в выводе).

### Шаг 4: Настроить туннель

Создайте директорию для конфигурации:

```bash
mkdir -p ~/.cloudflared
```

Создайте файл конфигурации `/root/.cloudflared/config.yml`:

```yaml
tunnel: telegram-shop
credentials-file: /root/.cloudflared/туннель-id.json

ingress:
  - hostname: telegram-shop-ваш-id.trycloudflare.com
    service: http://localhost:80
  - service: http_status:404
```

**Важно:** Замените `туннель-id.json` на реальный ID файла (он будет в `/root/.cloudflared/` после создания туннеля).

### Шаг 5: Запустить туннель

```bash
# Запустить туннель
cloudflared tunnel run telegram-shop
```

Вы получите URL вида:
```
https://telegram-shop-ваш-id.trycloudflare.com
```

### Шаг 6: Настроить автозапуск (опционально)

Создайте systemd service для автозапуска:

```bash
# Создать service файл
sudo nano /etc/systemd/system/cloudflared.service
```

Добавьте:

```ini
[Unit]
Description=Cloudflare Tunnel
After=network.target

[Service]
Type=simple
User=root
ExecStart=/usr/local/bin/cloudflared tunnel run telegram-shop
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
```

Запустить service:

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable cloudflared

# Запустить service
sudo systemctl start cloudflared

# Проверить статус
sudo systemctl status cloudflared
```

## 📝 Использование

### 1. Обновить .env

```env
VITE_API_URL=https://telegram-shop-ваш-id.trycloudflare.com
```

### 2. Обновить BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите "Bot Settings" → "Menu Button"
5. Укажите URL: `https://telegram-shop-ваш-id.trycloudflare.com`

### 3. Пересобрать и загрузить

```bash
npm run build
# Загрузите файлы из dist/ на сервер
```

## ✅ Преимущества Cloudflare Tunnel

1. **Бесплатно** - не нужно покупать домен
2. **Валидный SSL** - автоматически предоставляется Cloudflare
3. **Работает с IP** - не нужно настраивать DNS
4. **Безопасно** - трафик идет через Cloudflare
5. **Просто** - настройка за 5 минут

## ⚠️ Важно

1. **URL может измениться** - если перезапустить туннель, URL может измениться (для постоянного URL нужен домен)
2. **Для продакшена лучше использовать домен** - но для тестирования и разработки это отлично подходит
3. **Туннель должен быть запущен** - если туннель остановится, приложение не будет доступно

## 🔄 Обновление URL

Если URL изменился:

1. Обновите URL в BotFather
2. Обновите .env файл
3. Пересоберите приложение

## 🆘 Решение проблем

### Туннель не запускается

```bash
# Проверить логи
sudo journalctl -u cloudflared -f

# Проверить конфигурацию
cloudflared tunnel info telegram-shop
```

### URL не работает

```bash
# Проверить, что туннель запущен
sudo systemctl status cloudflared

# Проверить, что Nginx работает
sudo systemctl status nginx
```

### Порт занят

```bash
# Проверить, какой процесс использует порт 80
sudo lsof -i :80

# Остановить процесс или изменить порт в конфигурации
```

## 📚 Дополнительная информация

- Документация Cloudflare Tunnel: https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/
- Обсуждение на форуме: https://community.cloudflare.com/

