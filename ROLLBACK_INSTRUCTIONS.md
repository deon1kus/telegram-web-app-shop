# 🔄 Инструкции по откату изменений

## Чекпоинты для отката

### Чекпоинт 1: Начало интеграции с каналом
```bash
git tag checkpoint-channel-integration-start
```

**Откат к этому чекпоинту:**
```bash
git reset --hard checkpoint-channel-integration-start
```

---

## Откат последнего коммита

### Мягкий откат (сохраняет изменения в рабочей директории)
```bash
git reset --soft HEAD~1
```

### Откат с удалением изменений
```bash
git reset --hard HEAD~1
```

---

## Откат конкретного файла

```bash
# Откат одного файла
git checkout HEAD -- path/to/file

# Откат нескольких файлов
git checkout HEAD -- scripts/get-channel-message-cjs.js scripts/test-real-message.js
```

---

## Просмотр истории изменений

```bash
# Последние коммиты
git log --oneline -10

# Детали конкретного коммита
git show <commit-hash>

# Список всех тегов (чекпоинтов)
git tag -l
```

---

## Восстановление удаленных файлов

```bash
# Найти удаленный файл
git log --all --full-history -- path/to/file

# Восстановить файл из конкретного коммита
git checkout <commit-hash> -- path/to/file
```

---

## Откат через GitHub

Если изменения уже отправлены в GitHub:

1. **Откат последнего коммита:**
```bash
git reset --hard HEAD~1
git push origin main --force
```

⚠️ **ВНИМАНИЕ:** `--force` перезаписывает историю на GitHub. Используйте осторожно!

2. **Создание revert коммита (безопаснее):**
```bash
git revert HEAD
git push origin main
```

---

## Восстановление из резервной копии

Если есть локальная резервная копия:

```bash
# Копировать из резервной копии
cp -r F:\WebApp\telegram-web-app-shop-backup-v002\* F:\WebApp\telegram-web-app-shop\
```

---

## Логи для диагностики

Все логи сохраняются в:
- `scripts/channel-integration-steps.log` - детальный лог всех шагов
- `scripts/channel-message-result.json` - результат парсинга сообщения

**Просмотр логов:**
```bash
# Последние 50 строк лога
tail -n 50 scripts/channel-integration-steps.log

# Поиск ошибок в логе
grep ERROR scripts/channel-integration-steps.log
```

---

## Быстрый откат всех изменений

```bash
# Отменить все незакоммиченные изменения
git reset --hard HEAD

# Очистить неотслеживаемые файлы (кроме .gitignore)
git clean -fd
```

---

## Восстановление после ошибки

Если что-то пошло не так:

1. **Проверьте текущий статус:**
```bash
git status
git log --oneline -5
```

2. **Определите до какого коммита откатываться:**
```bash
git log --oneline
```

3. **Откатитесь:**
```bash
git reset --hard <commit-hash>
```

4. **Проверьте что все восстановлено:**
```bash
git status
```

---

## Контакты для помощи

Если откат не помог:
1. Проверьте логи: `scripts/channel-integration-steps.log`
2. Проверьте резервную копию: `F:\WebApp\telegram-web-app-shop-backup-v002`
3. Проверьте историю Git: `git log --oneline`

---

**Последнее обновление:** 2025-01-XX

