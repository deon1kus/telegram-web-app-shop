# Быстрая настройка Netlify (3 шага)

## ✅ Что уже сделано

1. ✅ Создан `netlify.toml` - конфигурация для Netlify
2. ✅ Создан `public/_redirects` - для работы SPA роутинга
3. ✅ Проект уже в GitHub: `deon1kus/telegram-web-app-shop`

## 🚀 Что нужно сделать

### Шаг 1: Подключить проект к Netlify

1. Зайдите на https://app.netlify.com
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и авторизуйтесь
4. Выберите репозиторий: **telegram-web-app-shop**
5. Нажмите **"Deploy site"**

Netlify автоматически:
- Установит зависимости
- Соберет проект
- Опубликует его

### Шаг 2: Настроить переменную окружения

1. В Netlify: **Site settings** → **Environment variables**
2. Нажмите **"Add variable"**
3. Добавьте:
   - **Key:** `VITE_API_URL`
   - **Value:** `http://194.87.0.193`
4. Нажмите **"Save"**
5. Пересоберите сайт: **Deploys** → **Trigger deploy** → **Deploy site**

### Шаг 3: Обновить BotFather

1. Дождитесь завершения деплоя
2. Скопируйте URL сайта (например: `https://telegram-shop-123456.netlify.app`)
3. Откройте @BotFather в Telegram
4. Отправьте `/mybots`
5. Выберите **J.A.R.V.I.S.**
6. Выберите **"Bot Settings"** → **"Menu Button"**
7. Укажите URL: `https://ваш-сайт.netlify.app`

## ✅ Готово!

Теперь приложение будет работать на мобильных устройствах с HTTPS!

## 🔄 Автоматический деплой

После настройки, каждый `git push` в GitHub автоматически обновит сайт на Netlify.

## ⚠️ Если API запросы не работают

Если ваш API на HTTP (без SSL), Netlify может блокировать запросы. Решение:

1. Откройте `netlify.toml`
2. Раскомментируйте секцию проксирования:

```toml
[[redirects]]
  from = "/api/*"
  to = "http://194.87.0.193/api/:splat"
  status = 200
  force = true
```

3. В Netlify удалите переменную `VITE_API_URL` (или оставьте пустой)
4. Пересоберите сайт

Теперь все запросы к `/api/*` будут проксироваться на ваш сервер через Netlify.

## 📝 Проверка

1. Откройте сайт в браузере
2. Откройте консоль (F12)
3. Проверьте, что нет ошибок CORS
4. Проверьте работу API запросов
5. Откройте в Telegram - должно работать!

