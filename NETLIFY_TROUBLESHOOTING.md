# 🔧 Решение проблем с Netlify

## Проблема: Сайт не открывается или не грузится

### Возможные причины и решения:

### 1. ✅ Конфликт редиректов

**Проблема:** Дублирование редиректов в `public/_redirects` и `netlify.toml`

**Решение:** 
- Редиректы настроены в `netlify.toml` (приоритет)
- Файл `public/_redirects` оставлен для совместимости, но не используется

### 2. ✅ Проверка сборки

Проверьте логи сборки в Netlify:
1. Зайдите в Netlify Dashboard
2. Откройте ваш проект
3. Перейдите в **Deploys**
4. Откройте последний деплой
5. Проверьте **Build log**

**Возможные ошибки:**
- Ошибки компиляции TypeScript
- Отсутствующие зависимости
- Ошибки сборки Vite

### 3. ✅ Проверка путей к файлам

Убедитесь что:
- `base: "/"` в `vite.config.ts` ✅
- `publish = "dist"` в `netlify.toml` ✅
- Все статические файлы в `public/` ✅

### 4. ✅ Проверка консоли браузера

Откройте консоль браузера (F12) и проверьте:
- Ошибки JavaScript
- 404 ошибки для файлов
- CORS ошибки
- Ошибки загрузки модулей

### 5. ✅ Проверка API проксирования

Если API запросы не работают:
1. Проверьте что API редирект ПЕРВЫЙ в `netlify.toml`
2. Проверьте что ваш сервер доступен: `http://194.87.0.193`
3. Проверьте логи Netlify для ошибок проксирования

### 6. ✅ Очистка кеша

Попробуйте:
1. Очистить кеш браузера (Ctrl+Shift+Delete)
2. Открыть в режиме инкогнито
3. Пересобрать проект в Netlify (Trigger deploy)

### 7. ✅ Проверка переменных окружения

В Netlify:
1. Site settings → Environment variables
2. Убедитесь что `VITE_API_URL` НЕ установлена (используется проксирование)
3. Если установлена - удалите её

### 8. ✅ Проверка файла telegram.js

Убедитесь что:
- Файл `public/telegram.js` существует
- Файл доступен по пути `/telegram.js`
- Скрипт загружается (проверьте Network tab)

## 🚀 Быстрое решение

1. **Пересоберите проект:**
   - В Netlify: Deploys → Trigger deploy → Deploy site

2. **Проверьте логи:**
   - Откройте последний деплой
   - Проверьте Build log на ошибки

3. **Проверьте консоль браузера:**
   - Откройте сайт
   - F12 → Console
   - Проверьте ошибки

4. **Очистите кеш:**
   - Ctrl+Shift+Delete
   - Очистите кеш и cookies

## 📋 Чеклист проверки

- [ ] Сборка прошла успешно (нет ошибок в Build log)
- [ ] Все файлы загружаются (проверьте Network tab)
- [ ] Нет ошибок в консоли браузера
- [ ] Редиректы настроены правильно
- [ ] API проксирование работает
- [ ] telegram.js загружается

## 🔍 Детальная диагностика

### Проверка в браузере:

1. Откройте сайт
2. F12 → Network tab
3. Обновите страницу
4. Проверьте:
   - Загружается ли `index.html`? (статус 200)
   - Загружаются ли JS файлы? (статус 200)
   - Загружается ли `telegram.js`? (статус 200)
   - Есть ли 404 ошибки?

### Проверка в Netlify:

1. Deploys → последний деплой
2. Проверьте:
   - Build status: Published ✅
   - Build time: не слишком долго
   - Build log: нет ошибок

## ✅ Если ничего не помогает

1. Проверьте что репозиторий подключен правильно
2. Проверьте что ветка `main` используется
3. Попробуйте пересоздать сайт в Netlify
4. Проверьте что все файлы закоммичены в Git

