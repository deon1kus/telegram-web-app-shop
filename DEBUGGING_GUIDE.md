# 🔍 Руководство по отладке проблем с загрузкой

## Проблема: Сайт не открывается

### Шаг 1: Проверка в браузере

1. **Откройте сайт:** https://webappshop1.netlify.app/
2. **Откройте консоль:** F12 → вкладка **Console**
3. **Проверьте ошибки:**
   - Красные ошибки (Errors)
   - Желтые предупреждения (Warnings)
   - Скопируйте все ошибки

### Шаг 2: Проверка Network

1. **F12 → вкладка Network**
2. **Обновите страницу** (F5)
3. **Проверьте:**
   - Загружается ли `index.html`? (статус должен быть 200)
   - Загружаются ли JS файлы? (статус должен быть 200)
   - Загружается ли `telegram.js`? (статус должен быть 200)
   - Есть ли 404 ошибки?

### Шаг 3: Проверка в Netlify

1. **Зайдите в Netlify Dashboard**
2. **Откройте проект webappshop1**
3. **Deploys → последний деплой**
4. **Проверьте:**
   - Статус: **Published** ✅
   - Build log: нет ошибок
   - Deploy log: нет ошибок

### Шаг 4: Типичные проблемы и решения

#### Проблема 1: Белый экран

**Причины:**
- Ошибка JavaScript
- Не загружаются файлы
- Проблема с путями

**Решение:**
1. Откройте консоль (F12)
2. Проверьте ошибки
3. Проверьте Network tab

#### Проблема 2: "Загрузка..." не исчезает

**Причины:**
- Ошибка инициализации
- Проблема с Telegram WebApp
- Ошибка в коде

**Решение:**
1. Проверьте консоль на ошибки
2. Проверьте что `isReady` становится `true`
3. Проверьте что нет ошибок в `useEffect`

#### Проблема 3: Ошибки загрузки файлов (404)

**Причины:**
- Неправильные пути к файлам
- Файлы не собраны
- Проблема с редиректами

**Решение:**
1. Проверьте что все файлы в `dist/`
2. Проверьте пути в Network tab
3. Проверьте `netlify.toml` редиректы

#### Проблема 4: CORS ошибки

**Причины:**
- API запросы блокируются
- Проблема с проксированием

**Решение:**
1. Проверьте что API проксирование настроено
2. Проверьте что редирект `/api/*` первый в `netlify.toml`
3. Проверьте что ваш сервер доступен

### Шаг 5: Диагностика

Откройте консоль браузера и выполните:

```javascript
// Проверка базового URL
console.log('Base URL:', window.location.origin);

// Проверка root элемента
console.log('Root element:', document.getElementById('root'));

// Проверка Telegram
console.log('Telegram WebApp:', window.Telegram);

// Проверка загруженных скриптов
console.log('Scripts:', document.querySelectorAll('script'));
```

### Шаг 6: Что проверить в коде

1. **index.html:**
   - Есть ли `<div id="root"></div>`?
   - Правильный ли путь к `main.tsx`?

2. **main.tsx:**
   - Правильно ли импортируется App?
   - Есть ли обработка ошибок?

3. **App.tsx:**
   - Правильно ли инициализируется?
   - Есть ли обработка ошибок?

### Шаг 7: Быстрое решение

1. **Очистите кеш браузера:**
   - Ctrl+Shift+Delete
   - Очистите кеш и cookies

2. **Откройте в режиме инкогнито:**
   - Ctrl+Shift+N (Chrome)
   - Ctrl+Shift+P (Firefox)

3. **Пересоберите проект:**
   - В Netlify: Deploys → Trigger deploy

### Шаг 8: Если ничего не помогает

1. **Проверьте логи Netlify:**
   - Deploys → последний деплой → Build log
   - Скопируйте все ошибки

2. **Проверьте консоль браузера:**
   - F12 → Console
   - Скопируйте все ошибки

3. **Проверьте Network:**
   - F12 → Network
   - Найдите запросы со статусом ошибки
   - Скопируйте детали

## 📋 Чеклист диагностики

- [ ] Сайт открывается в браузере?
- [ ] Нет ошибок в консоли?
- [ ] Все файлы загружаются (статус 200)?
- [ ] `index.html` загружается?
- [ ] JS файлы загружаются?
- [ ] `telegram.js` загружается?
- [ ] Нет 404 ошибок?
- [ ] Нет CORS ошибок?
- [ ] Деплой в Netlify успешен?

## 🔧 Полезные команды для отладки

### В консоли браузера:

```javascript
// Проверка окружения
console.log('Environment:', {
  origin: window.location.origin,
  pathname: window.location.pathname,
  hasRoot: !!document.getElementById('root'),
  hasTelegram: !!window.Telegram
});

// Проверка загруженных модулей
console.log('Loaded scripts:', Array.from(document.querySelectorAll('script')).map(s => s.src));

// Принудительная перезагрузка
window.location.reload();
```

## ✅ После исправлений

После применения исправлений:
1. Дождитесь автоматического деплоя (2-3 минуты)
2. Очистите кеш браузера
3. Откройте сайт заново
4. Проверьте консоль на ошибки

