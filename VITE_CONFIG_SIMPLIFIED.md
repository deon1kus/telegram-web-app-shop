# 🔧 Упрощение vite.config.ts

## Проблема

Приложение долго грузится и не загружается из-за сложной конфигурации chunk splitting, которая вызывает проблемы с загрузкой файлов.

## Решение

Упрощена конфигурация `vite.config.ts` до минимальной, как в оригинальном репозитории [mojtaba1180/telegram-web-app-shop](https://github.com/mojtaba1180/telegram-web-app-shop).

### Что удалено:

1. ❌ Сложный `manualChunks` - вызывал проблемы с загрузкой
2. ❌ `rollupOptions` с кастомным chunk splitting
3. ❌ `terserOptions` - используем дефолтные настройки
4. ❌ `commonjsOptions` - не нужны
5. ❌ `target: 'esnext'` - используем дефолт
6. ❌ `cssCodeSplit: true` - используем дефолт
7. ❌ `assetsInlineLimit` - используем дефолт
8. ❌ `emptyOutDir` - используем дефолт

### Что оставлено:

1. ✅ Базовые настройки плагинов
2. ✅ `outDir: "dist"`
3. ✅ `assetsDir: "assets"`
4. ✅ `sourcemap: false` - для продакшена
5. ✅ `chunkSizeWarningLimit: 1000` - увеличен лимит
6. ✅ `base: "/"` - для Netlify

## Результат

Теперь Vite использует дефолтную стратегию chunk splitting, которая работает надежнее и не вызывает проблем с загрузкой.

## Проверка

После деплоя проверьте:
1. Откройте сайт: https://celadon-starburst-162af0.netlify.app/
2. Откройте DevTools (F12) → Network
3. Обновите страницу (Ctrl+R)
4. Проверьте, что все файлы загружаются без ошибок

