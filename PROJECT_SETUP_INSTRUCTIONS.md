# 📋 Инструкции по настройке проекта v0.0.2

## 🎯 Цель документа

Этот документ содержит все необходимые инструкции для повторной настройки проекта с нуля без ошибок, которые встречались ранее.

---

## 📦 Шаг 1: Клонирование проекта

```bash
# Клонируем проект с GitHub
git clone https://github.com/deon1kus/telegram-web-app-shop.git
cd telegram-web-app-shop

# Проверяем версию
cat VERSION
# Должно быть: 0.0.2
```

---

## 🔧 Шаг 2: Установка зависимостей

```bash
# Устанавливаем зависимости
npm install

# Проверяем что все установилось
npm list --depth=0
```

**Важно:** Если возникают ошибки при установке:
- Убедитесь что Node.js версии 18.x или выше
- Удалите `node_modules` и `package-lock.json`, затем повторите `npm install`

---

## ⚙️ Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# URL вашего API сервера
VITE_API_URL=http://194.87.0.193
```

**⚠️ ВАЖНО:** 
- В продакшене на Netlify НЕ добавляйте эту переменную!
- Используется проксирование через `netlify.toml`

---

## 🏗️ Шаг 4: Проверка критичных файлов

### 4.1. vite.config.ts

**КРИТИЧНО:** Chunk splitting должен быть настроен правильно!

```typescript
// ✅ ПРАВИЛЬНО: Все React-зависимости в основном бандле
manualChunks: (id) => {
  if (id.includes('node_modules')) {
    const isReactRelated = 
      id.includes('react') ||
      id.includes('scheduler') ||
      // ... все React-зависимости
    
    if (!isReactRelated) {
      return 'vendor'; // Только чистые библиотеки
    }
    return undefined; // React в основном бандле
  }
}
```

**❌ НЕПРАВИЛЬНО:** Разделение React на отдельный chunk приведет к ошибке:
```
Cannot read properties of undefined (reading 'useState')
```

### 4.2. index.html

**КРИТИЧНО:** `telegram.js` должен загружаться синхронно!

```html
<!-- ✅ ПРАВИЛЬНО: Без defer -->
<script src="/telegram.js"></script>

<!-- ❌ НЕПРАВИЛЬНО: С defer -->
<script src="/telegram.js" defer></script>
```

### 4.3. src/layouts/main.tsx

**КРИТИЧНО:** Направление и локаль должны быть правильными!

```typescript
// ✅ ПРАВИЛЬНО: LTR и русская локаль
<ConfigProvider
  direction="ltr"
  locale={ru_RU}
  ...
>

// ❌ НЕПРАВИЛЬНО: RTL и персидская локаль
<ConfigProvider
  direction="rtl"
  locale={fa_IR}
  ...
>
```

### 4.4. src/hooks/useTelegramUser.ts

**КРИТИЧНО:** Используйте безопасное обращение к данным!

```typescript
// ✅ ПРАВИЛЬНО: Опциональное обращение
const telegramUser = useTelegramUser();
const id = telegramUser?.id;

// ❌ НЕПРАВИЛЬНО: Прямое деструктурирование
const { id } = useTelegramUser(); // Может быть null!
```

---

## 🧪 Шаг 5: Локальная проверка

```bash
# Запускаем dev сервер
npm run dev

# Открываем в браузере
# http://localhost:3000

# Проверяем консоль браузера на ошибки
# Должно быть чисто, без ошибок
```

**Что проверить:**
- ✅ Приложение загружается без ошибок
- ✅ Нет ошибок в консоли
- ✅ Текст вводится слева направо
- ✅ Календарь на русском языке
- ✅ Кнопки видны и кликабельны

---

## 🚀 Шаг 6: Сборка для продакшена

```bash
# Собираем проект
npm run build

# Проверяем что dist/ создан
ls dist/

# Предпросмотр сборки
npm run prev
```

**Что проверить после сборки:**
- ✅ Папка `dist/` создана
- ✅ Файлы `index.html` и `assets/` присутствуют
- ✅ Нет ошибок при сборке
- ✅ Размер бандлов разумный (< 1MB для основного)

---

## 📤 Шаг 7: Развертывание на Netlify

### 7.1. Подключение репозитория

1. Войдите в Netlify
2. Нажмите "Add new site" → "Import an existing project"
3. Выберите GitHub и репозиторий `telegram-web-app-shop`
4. Netlify автоматически определит настройки из `netlify.toml`

### 7.2. Настройки сборки

**ВАЖНО:** НЕ добавляйте переменную `VITE_API_URL` в Netlify!

Настройки должны быть:
- **Build command:** `npm run build`
- **Publish directory:** `dist`
- **Node version:** 18

### 7.3. Проверка после деплоя

1. Откройте URL вашего сайта
2. Проверьте консоль браузера
3. Проверьте что API запросы работают
4. Проверьте что приложение открывается в Telegram

---

## 🤖 Шаг 8: Настройка бота

### 8.1. Обновление URL в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите ваш бот
4. Выберите "Bot Settings" → "Menu Button"
5. Укажите URL: `https://your-site.netlify.app`

### 8.2. Обновление на сервере

```bash
# Подключаемся к серверу
ssh root@194.87.0.193

# Переходим в директорию бота
cd /root/telegram-bot

# Редактируем .env
nano .env

# Обновляем SHOP_URL
SHOP_URL=https://your-new-url.netlify.app

# Сохраняем и перезапускаем бота
pm2 restart telegram-bot
```

---

## ⚠️ Известные проблемы и решения

### Проблема 1: "Cannot destructure property 'id' of 'Xn(...)' as it is null"

**Причина:** Прямое деструктурирование из `useTelegramUser()`

**Решение:**
```typescript
// Используйте опциональное обращение
const telegramUser = useTelegramUser();
const id = telegramUser?.id;
```

### Проблема 2: Черный экран при загрузке

**Причина:** Неправильный chunk splitting или `defer` в `telegram.js`

**Решение:**
1. Проверьте `vite.config.ts` - все React в основном бандле
2. Проверьте `index.html` - `telegram.js` без `defer`

### Проблема 3: Текст вводится справа налево

**Причина:** RTL направление в `ConfigProvider`

**Решение:**
```typescript
// В src/layouts/main.tsx
<ConfigProvider direction="ltr" locale={ru_RU} ...>
```

### Проблема 4: Персидский календарь

**Причина:** Использование `antd-jalali` и `jalali-moment`

**Решение:**
```typescript
// Используйте стандартный DatePicker
import { DatePicker } from "antd";
import dayjs from "dayjs";
// НЕ используйте antd-jalali и jalali-moment
```

### Проблема 5: Кнопки невидимы на темной теме

**Причина:** Прозрачный фон кнопок

**Решение:**
```css
/* В src/assets/styles/cyberpunk.css */
.ant-btn {
  background: #e5e5e5 !important; /* Непрозрачный серый */
  color: #000000 !important;
}
```

---

## 📝 Структура проекта после очистки

```
telegram-web-app-shop/
├── src/                    # Исходный код
│   ├── components/         # Компоненты
│   ├── pages/              # Страницы
│   ├── framework/          # API
│   ├── layouts/            # Макеты
│   ├── hooks/              # Хуки
│   └── assets/             # Ресурсы
├── public/                 # Публичные файлы
├── scripts/                # Скрипты
├── dist/                   # Сборка (не коммитится)
├── .env                    # Переменные окружения (не коммитится)
├── .gitignore              # Git ignore
├── netlify.toml            # Конфигурация Netlify
├── vite.config.ts          # Конфигурация Vite
├── package.json            # Зависимости
├── VERSION                 # Версия проекта (0.0.2)
├── README.md               # Основная документация
├── CHANGELOG.md            # История изменений
├── DEPLOYMENT.md           # Инструкции по развертыванию
├── BOT_CONFIG.md           # Конфигурация бота
├── ADMIN_GUIDE.md          # Руководство по админке
└── PROJECT_SETUP_INSTRUCTIONS.md  # Этот файл
```

---

## 🔄 Резервное копирование

### Создание резервной копии

```bash
# На вашем ПК
cd F:\WebApp
cp -r telegram-web-app-shop telegram-web-app-shop-backup-v002
# Или на Windows:
xcopy telegram-web-app-shop telegram-web-app-shop-backup-v002 /E /I
```

### Восстановление из резервной копии

```bash
# Удаляем текущую версию
rm -rf telegram-web-app-shop

# Восстанавливаем из резервной копии
cp -r telegram-web-app-shop-backup-v002 telegram-web-app-shop
```

---

## ✅ Чеклист перед развертыванием

- [ ] Версия проекта: 0.0.2
- [ ] Все зависимости установлены (`npm install`)
- [ ] `.env` файл создан с правильным `VITE_API_URL`
- [ ] `vite.config.ts` настроен правильно (React в основном бандле)
- [ ] `index.html` без `defer` для `telegram.js`
- [ ] `src/layouts/main.tsx` с `direction="ltr"` и `locale={ru_RU}`
- [ ] Все использования `useTelegramUser()` с опциональным обращением
- [ ] Локальная проверка прошла успешно (`npm run dev`)
- [ ] Сборка прошла успешно (`npm run build`)
- [ ] Нет ошибок в консоли браузера
- [ ] Бот настроен в BotFather
- [ ] URL обновлен на сервере

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте этот документ
2. Проверьте [DEPLOYMENT.md](./DEPLOYMENT.md)
3. Проверьте [CHANGELOG.md](./CHANGELOG.md)
4. Проверьте консоль браузера
5. Проверьте логи Netlify

---

**Версия документа:** 0.0.2  
**Последнее обновление:** 2025-01-XX

