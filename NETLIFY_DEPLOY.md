# Деплой на Netlify - Полная инструкция

## ✅ Преимущества использования Netlify

1. **Автоматический HTTPS** - Netlify предоставляет SSL сертификат бесплатно
2. **CDN** - Быстрая загрузка по всему миру
3. **Автоматический деплой** - При пуше в GitHub автоматически обновляется
4. **Бесплатно** - Для небольших проектов достаточно бесплатного плана
5. **Простота** - Не нужно настраивать SSL на сервере

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Убедиться, что проект в GitHub

Проект уже должен быть в GitHub: `https://github.com/deon1kus/telegram-web-app-shop`

### Шаг 2: Подключить проект к Netlify

1. Зайдите на https://app.netlify.com
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub**
4. Авторизуйтесь, если нужно
5. Выберите репозиторий: `telegram-web-app-shop`
6. Нажмите **"Deploy site"**

### Шаг 3: Настроить переменные окружения

1. В Netlify перейдите в **Site settings** → **Environment variables**
2. Добавьте переменную:
   - **Key:** `VITE_API_URL`
   - **Value:** `http://194.87.0.193` (или `https://194.87.0.193` если настроен HTTPS)

**Важно:** Если ваш API на HTTP, Netlify может блокировать запросы. В этом случае:
- Используйте проксирование через Netlify (см. ниже)
- Или настройте HTTPS на сервере

### Шаг 4: Настроить Build settings

Netlify автоматически определит настройки из `netlify.toml`, но можно проверить:

1. **Site settings** → **Build & deploy** → **Build settings**
2. **Build command:** `npm run build`
3. **Publish directory:** `dist`

### Шаг 5: Дождаться деплоя

Netlify автоматически:
1. Установит зависимости (`npm install`)
2. Соберет проект (`npm run build`)
3. Опубликует файлы из `dist/`

### Шаг 6: Получить URL

После деплоя вы получите URL вида:
- `https://random-name-123456.netlify.app`
- Или можете настроить свой домен

### Шаг 7: Обновить BotFather

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите бота **J.A.R.V.I.S.**
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Укажите URL: `https://ваш-сайт.netlify.app`

## 🔧 Настройка проксирования API (если API на HTTP)

Если ваш API на HTTP (без SSL), Netlify может блокировать запросы. Решение - проксировать через Netlify:

### Вариант 1: Проксирование через Netlify (рекомендуется)

Раскомментируйте в `netlify.toml`:

```toml
[[redirects]]
  from = "/api/*"
  to = "http://194.87.0.193/api/:splat"
  status = 200
  force = true
```

И обновите `.env` в Netlify:
- **Key:** `VITE_API_URL`
- **Value:** (оставьте пустым или удалите)

Приложение будет использовать относительные пути `/api/*`, которые будут проксироваться на ваш сервер.

### Вариант 2: Настроить CORS на сервере

Если хотите делать запросы напрямую, настройте CORS на вашем бэкенде:

```javascript
// Пример для Express.js
app.use(cors({
  origin: ['https://ваш-сайт.netlify.app'],
  credentials: true
}));
```

## 📝 Настройка кастомного домена (опционально)

1. В Netlify: **Site settings** → **Domain management**
2. Нажмите **"Add custom domain"**
3. Введите ваш домен
4. Настройте DNS записи согласно инструкциям Netlify

## 🔄 Автоматический деплой

После настройки, каждый раз когда вы делаете `git push` в GitHub:
1. Netlify автоматически обнаружит изменения
2. Запустит сборку
3. Опубликует новую версию

## ⚙️ Переменные окружения в Netlify

Для разных окружений можно настроить разные переменные:

1. **Site settings** → **Environment variables**
2. Добавьте переменные:
   - **Production:** `VITE_API_URL` = `http://194.87.0.193`
   - **Deploy previews:** `VITE_API_URL` = `http://194.87.0.193` (или другой для тестирования)

## 🐛 Решение проблем

### Проблема: "Build failed"

**Решение:**
1. Проверьте логи сборки в Netlify
2. Убедитесь, что `package.json` содержит все зависимости
3. Проверьте, что Node.js версия правильная (18+)

### Проблема: "API requests blocked"

**Решение:**
1. Используйте проксирование через Netlify (см. выше)
2. Или настройте CORS на сервере
3. Или настройте HTTPS на сервере

### Проблема: "404 на всех страницах"

**Решение:**
1. Убедитесь, что файл `public/_redirects` существует
2. Проверьте настройки в `netlify.toml`

### Проблема: "Environment variables not working"

**Решение:**
1. Переменные должны начинаться с `VITE_` для Vite
2. После изменения переменных нужно пересобрать сайт
3. Проверьте, что переменные добавлены в правильное окружение

## 📋 Чеклист деплоя

- [ ] Проект загружен в GitHub
- [ ] Создан `netlify.toml`
- [ ] Создан `public/_redirects`
- [ ] Проект подключен к Netlify
- [ ] Настроена переменная `VITE_API_URL`
- [ ] Деплой успешно завершен
- [ ] Получен HTTPS URL от Netlify
- [ ] URL обновлен в BotFather
- [ ] Приложение работает на мобильном

## 🎯 Итоговая архитектура

```
┌─────────────────┐
│   Telegram      │
│   Web App       │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────┐
│    Netlify      │
│  (Frontend)     │
│  HTTPS + CDN    │
└────────┬────────┘
         │ HTTP/HTTPS
         ▼
┌─────────────────┐
│  Ваш сервер     │
│  194.87.0.193   │
│  (Backend API)  │
└─────────────────┘
```

## 🚀 После деплоя

1. **Проверьте работу:**
   - Откройте URL в браузере
   - Проверьте консоль на ошибки
   - Проверьте работу API запросов

2. **Обновите BotFather:**
   - Используйте Netlify URL
   - Приложение должно работать на мобильных!

3. **Настройте автодеплой:**
   - Каждый `git push` будет автоматически обновлять сайт

## 💡 Дополнительные возможности

- **Deploy previews** - автоматические превью для каждого PR
- **Branch deploys** - деплой разных веток
- **Split testing** - A/B тестирование
- **Forms** - обработка форм (если нужно)
- **Functions** - serverless функции (если нужно)

