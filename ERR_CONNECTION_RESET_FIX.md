# 🔧 Исправление ERR_CONNECTION_RESET ошибок

## Проблема

Ошибки `net::ERR_CONNECTION_RESET` для файлов `vendor-*.js` и `index-*.js` возникают из-за:
1. Слишком больших чанков JavaScript
2. Множества параллельных запросов
3. Таймаутов на стороне Netlify
4. Проблем с загрузкой больших файлов

## Решение

### 1. Упрощенный Chunk Splitting

**Изменения в `vite.config.ts`:**
- Минимальное разделение чанков (только React и Ant Design отдельно)
- Все остальные библиотеки в один `vendor` chunk
- Это уменьшает количество параллельных запросов

### 2. Удален defer из telegram.js

**Изменения в `index.html`:**
- `telegram.js` теперь загружается синхронно (без `defer`)
- Это гарантирует, что Telegram API доступен до загрузки React

### 3. Оптимизация Netlify

**Изменения в `netlify.toml`:**
- Увеличен лимит памяти для Node.js
- Добавлен `Connection: keep-alive` для статических файлов

## Что было исправлено

1. ✅ Упрощен chunk splitting - меньше чанков = меньше запросов
2. ✅ Удален defer из telegram.js - синхронная загрузка
3. ✅ Оптимизированы настройки Netlify
4. ✅ Уменьшен размер чанков

## Проверка

После деплоя проверьте:
1. Откройте сайт: https://celadon-starburst-162af0.netlify.app/
2. Откройте DevTools (F12) → Network
3. Проверьте, что все файлы загружаются без ошибок
4. Проверьте Console на наличие ошибок

## Если ошибки остаются

1. Очистите кеш браузера (Ctrl+Shift+Delete)
2. Очистите кеш Netlify (Site settings → Build & deploy → Clear cache)
3. Пересоберите проект вручную
4. Проверьте размер чанков - они не должны превышать 500KB

## Примечание об ошибке Reviews

Ошибка `ReferenceError: Reviews is not defined` может быть связана с:
- Внешним скриптом, который пытается использовать Reviews
- Старым кешем браузера
- Проблемой с загрузкой чанков

После исправления chunk splitting эта ошибка должна исчезнуть.

