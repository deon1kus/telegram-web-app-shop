# Настройка API URL

## ✅ Файл .env создан

Файл `.env` уже создан в корне проекта. Теперь нужно указать правильный URL вашего API сервера.

## 🔍 Как узнать URL вашего API?

### Вариант 1: Если у вас уже есть работающий бэкенд

1. Откройте файл `.env` в корне проекта
2. Найдите строку: `VITE_API_URL=https://your-api-server.com`
3. Замените `https://your-api-server.com` на реальный URL вашего API

**Примеры:**
- Если API на том же сервере: `VITE_API_URL=https://yourdomain.com`
- Если API на отдельном поддомене: `VITE_API_URL=https://api.yourdomain.com`
- Если API на другом домене: `VITE_API_URL=https://backend.example.com`
- Для локальной разработки: `VITE_API_URL=http://localhost:8000`

### Вариант 2: Если вы не знаете URL API

**Спросите у разработчика бэкенда или проверьте:**

1. **Документацию проекта** - там должен быть указан URL API
2. **Настройки сервера** - если у вас есть доступ к серверу
3. **Конфигурационные файлы** - проверьте другие файлы проекта на наличие URL

### Вариант 3: Если API еще не настроен

Если бэкенд API еще не готов:

1. Оставьте значение по умолчанию: `VITE_API_URL=https://your-api-server.com`
2. Приложение будет работать, но запросы к API будут падать с ошибками
3. Это нормально для разработки фронтенда

## 📝 Как изменить .env файл

1. Откройте файл `.env` в любом текстовом редакторе (Блокнот, VS Code и т.д.)
2. Найдите строку: `VITE_API_URL=https://your-api-server.com`
3. Замените на ваш реальный URL
4. Сохраните файл
5. **Важно:** Перезапустите dev сервер после изменения `.env`

## ⚠️ Важные замечания

1. **Не коммитьте .env в Git** - файл уже добавлен в `.gitignore`
2. **Используйте HTTPS для продакшена** - Telegram Web App требует HTTPS
3. **Проверьте CORS** - убедитесь, что ваш API разрешает запросы с вашего домена
4. **Перезапустите сервер** - после изменения `.env` нужно перезапустить `npm run dev`

## 🧪 Проверка настройки

После настройки URL:

1. Запустите приложение: `npm run dev`
2. Откройте консоль браузера (F12)
3. Перейдите на вкладку Network
4. Попробуйте выполнить какое-то действие в приложении
5. Проверьте запросы к API - они должны идти на ваш URL

## ❓ Что делать, если не знаете URL?

Если вы не знаете URL вашего API:

1. **Спросите у команды разработки** - у них должна быть эта информация
2. **Проверьте документацию проекта** - там может быть указан URL
3. **Используйте относительные пути** - оставьте пустым или используйте `/api` (работает только если API на том же домене)

## 🔄 После изменения .env

После изменения файла `.env`:

```bash
# Остановите текущий dev сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

Или для продакшена:

```bash
npm run build
```

