# 📢 Интеграция Telegram канала как бэкенда для товаров

## 🎯 Описание

Реализована полная интеграция Telegram канала как источника данных для товаров магазина. Система позволяет:
- Автоматически парсить посты из канала
- Извлекать информацию о товарах (название, цена, описание, фото, контакты)
- Публиковать товары в магазине через бота
- Автоматически синхронизировать новые посты

## 🏗️ Архитектура

### Компоненты системы

1. **Система логирования** (`src/utils/logger.ts`)
   - Централизованное логирование всех операций
   - Уровни: DEBUG, INFO, WARN, ERROR, SUCCESS
   - Хранение логов в памяти для отладки

2. **API для работы с каналом** (`src/framework/api/channel/`)
   - `get-posts.ts` - получение постов из канала
   - `parse-post.ts` - парсинг поста и извлечение данных товара
   - `sync.ts` - синхронизация канала и управление настройками
   - `publish-product.ts` - публикация товара в магазине

3. **Парсер постов** (`src/utils/channel-parser.ts`)
   - Извлечение названия товара
   - Парсинг цены (поддержка разных валют)
   - Извлечение описания
   - Поиск контактной информации (телефон, username, email)
   - Определение категорий по ключевым словам
   - Обработка фотографий

4. **Интерфейс управления** (`src/pages/bot/channel/index.tsx`)
   - Настройка канала (ID или username)
   - Включение/выключение автоматической синхронизации
   - Настройка интервала синхронизации
   - Просмотр постов из канала
   - Ручная публикация товаров

5. **Автоматическая синхронизация** (`src/hooks/useChannelAutoSync.ts`)
   - Периодическая проверка новых постов
   - Автоматическая публикация товаров
   - Настраиваемый интервал

## 📋 Типы данных

### TelegramChannelPost
```typescript
{
  message_id: number;
  channel_id: string;
  channel_username: string;
  date: number;
  text?: string;
  caption?: string;
  photos?: TelegramPhoto[];
  entities?: TelegramMessageEntity[];
  raw_data: unknown;
}
```

### ParsedProductFromChannel
```typescript
{
  product_name: string;
  description: string;
  price: number;
  quantity: number;
  photos: string[];
  contact_info?: {
    phone?: string;
    username?: string;
    email?: string;
  };
  category_ids?: number[];
  channel_message_id: number;
  channel_id: string;
  parsed_at: string;
  raw_text: string;
}
```

### ChannelSyncSettings
```typescript
{
  channel_id: string;
  channel_username: string;
  auto_sync: boolean;
  sync_interval_minutes: number;
  last_sync_message_id?: number;
  parse_rules: {
    price_patterns: string[];
    contact_patterns: string[];
    category_keywords: Record<string, number[]>;
  };
}
```

## 🔧 API Endpoints (Backend должен реализовать)

### GET `/api/channel/posts`
Получение постов из канала

**Параметры:**
- `channel_id` - ID канала
- `channel_username` - Username канала
- `limit` - Количество постов (по умолчанию 20)
- `offset` - Смещение
- `after_message_id` - Получить посты после указанного ID

**Ответ:**
```json
{
  "posts": TelegramChannelPost[]
}
```

### POST `/api/channel/parse-post`
Парсинг поста и извлечение данных товара

**Тело запроса:**
```json
{
  "post": TelegramChannelPost,
  "parse_rules": {
    "price_patterns": string[],
    "contact_patterns": string[],
    "category_keywords": Record<string, number[]>
  }
}
```

**Ответ:**
```json
ParsedProductFromChannel
```

### GET `/api/channel/sync-settings`
Получение настроек синхронизации

**Ответ:**
```json
ChannelSyncSettings | null
```

### POST `/api/channel/sync-settings`
Сохранение настроек синхронизации

**Тело запроса:**
```json
ChannelSyncSettings
```

**Ответ:**
```json
ChannelSyncSettings
```

### POST `/api/channel/sync`
Синхронизация канала

**Тело запроса:**
```json
{
  "channel_id": string,
  "channel_username": string,
  "limit": number,
  "force": boolean
}
```

**Ответ:**
```json
{
  "success": boolean,
  "message": string,
  "synced_count": number,
  "failed_count": number,
  "products": ParsedProductFromChannel[],
  "errors": Array<{
    "message_id": number,
    "error": string
  }>
}
```

### POST `/api/channel/publish-product`
Публикация товара из канала в магазин

**Тело запроса:**
```json
{
  "parsed_product": ParsedProductFromChannel,
  "user_id": string,
  "auto_publish": boolean
}
```

**Ответ:**
```json
{
  "success": boolean,
  "message": string,
  "product_id": number,
  "product": Product
}
```

## 🎨 Парсинг данных

### Извлечение названия
- Первая строка поста
- Удаление эмодзи и специальных символов
- Максимальная длина: 200 символов

### Извлечение цены
Поддерживаемые форматы:
- `1000 руб` / `1000 ₽` / `1000 RUB`
- `1000 тг` / `1000 ₸` / `1000 тенге` / `1000 KZT`
- `1000 сом` / `1000 KGS`
- `1000 USD` / `1000 $`
- `1000 EUR` / `1000 €`
- `цена: 1000` / `стоимость: 1000`

### Извлечение контактов
- Телефон: `+7 999 123 45 67`, `89991234567`
- Username: `@username`
- Email: `email@example.com`

### Извлечение категорий
По ключевым словам из настроек:
```typescript
{
  "электроника": [1, 2],
  "одежда": [3, 4],
  "еда": [5]
}
```

## 📝 Логирование

Все операции логируются с помощью системы `logger`:

```typescript
import { logInfo, logError, logSuccess } from "@utils/logger";

logInfo('ModuleName', 'Operation started', { data });
logSuccess('ModuleName', 'Operation completed', { result });
logError('ModuleName', 'Operation failed', error, { context });
```

### Уровни логирования:
- **DEBUG** - детальная информация (только в dev)
- **INFO** - общая информация
- **WARN** - предупреждения
- **ERROR** - ошибки (всегда логируются)
- **SUCCESS** - успешные операции

## 🚀 Использование

### 1. Настройка канала

1. Перейдите в `/bot/channel`
2. Укажите ID канала или username
3. Включите автоматическую синхронизацию (опционально)
4. Настройте интервал синхронизации
5. Сохраните настройки

### 2. Ручная синхронизация

1. Нажмите "Синхронизировать сейчас"
2. Система получит последние посты из канала
3. Посты отобразятся в таблице

### 3. Публикация товара

1. В таблице постов нажмите "Добавить в магазин"
2. Система распарсит пост
3. Товар будет добавлен в магазин

### 4. Автоматическая синхронизация

При включенной автоматической синхронизации:
- Система периодически проверяет новые посты
- Автоматически парсит и публикует товары
- Интервал настраивается в минутах

## 🔍 Валидация

Перед публикацией товар валидируется:
- Название: минимум 3 символа
- Цена: должна быть больше 0
- Количество: должно быть больше 0
- Описание: минимум 10 символов
- Фото: хотя бы одно фото

## 📊 Мониторинг

Все операции логируются. Для просмотра логов:
```typescript
import { logger } from "@utils/logger";

// Получить все логи
const logs = logger.getLogs();

// Получить логи по модулю
const channelLogs = logger.getLogsByModule('ChannelAPI');

// Получить ошибки
const errors = logger.getLogsByLevel(LogLevel.ERROR);

// Экспорт логов
const logsJson = logger.exportLogs();
```

## ⚙️ Настройка парсинга

В настройках синхронизации можно настроить правила парсинга:

```typescript
{
  price_patterns: [
    "\\d+\\s*руб",
    "\\d+\\s*₽"
  ],
  contact_patterns: [
    "\\+?\\d[\\d\\s\\-\\(\\)]{7,}"
  ],
  category_keywords: {
    "электроника": [1, 2],
    "одежда": [3]
  }
}
```

## 🔐 Безопасность

- Все операции требуют авторизации пользователя
- Логирование всех действий для аудита
- Валидация всех данных перед публикацией
- Обработка ошибок на всех уровнях

## 📈 Производительность

- Кеширование настроек (10 минут)
- Кеширование постов (5 минут)
- Ленивая загрузка данных
- Оптимизированные запросы к API

## 🐛 Отладка

Для отладки используйте:
1. Консоль браузера (F12) - все логи видны
2. Система логирования - централизованное хранение
3. Network tab - проверка API запросов

## ✅ Готово к использованию

Система полностью готова к использованию. Backend должен реализовать соответствующие API endpoints.



