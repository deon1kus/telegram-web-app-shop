# 🚀 Инструкция по развертыванию проекта v0.0.2

## 📋 Содержание
1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Развертывание на Netlify](#развертывание-на-netlify)
4. [Настройка бота](#настройка-бота)
5. [Известные проблемы и решения](#известные-проблемы-и-решения)
6. [Версия проекта](#версия-проекта)

---

## Требования

- **Node.js:** 18.x или выше
- **npm:** 9.x или выше
- **Git:** для работы с репозиторием
- **Netlify аккаунт:** для развертывания (или свой сервер)

---

## Быстрый старт

### 1. Клонирование проекта

```bash
git clone https://github.com/deon1kus/telegram-web-app-shop.git
cd telegram-web-app-shop
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# URL вашего API сервера
VITE_API_URL=http://194.87.0.193
```

**Важно:** В продакшене на Netlify переменная не нужна, используется проксирование.

### 4. Локальная разработка

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### 5. Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

---

## Развертывание на Netlify

### Автоматическое развертывание

1. Подключите репозиторий к Netlify
2. Netlify автоматически определит настройки из `netlify.toml`
3. Все изменения в `main` ветке автоматически деплоятся

### Конфигурация Netlify

- **Build command:** `npm run build`
- **Publish directory:** `dist`
- **Node version:** 18

### API проксирование

API запросы автоматически проксируются через Netlify:
- Запросы к `/api/*` проксируются на `http://194.87.0.193/api/*`
- Все запросы идут через HTTPS

**⚠️ ВАЖНО:** НЕ добавляйте переменную `VITE_API_URL` в Netlify! Используется проксирование.

---

## Настройка бота

### 1. Информация о боте

- **Название:** J.A.R.V.I.S.
- **Username:** @JARVIS_SHEVA_bot
- **Token:** `7811866862:AAH4z4mpba_o-fRCdgDv09Ej8nTy-QkzId8`
- **Канал уведомлений:** https://t.me/+vZtVvSSVltwzYmMy
- **Chat ID канала:** `-1003018207910`

### 2. Настройка в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите **J.A.R.V.I.S.** (@JARVIS_SHEVA_bot)
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Укажите URL вашего Web App:
   ```
   https://celadon-starburst-162af0.netlify.app
   ```

### 3. Обновление URL бота на сервере

Если URL изменился, обновите на сервере:

```bash
# На сервере (194.87.0.193)
cd /root/telegram-bot
nano .env

# Измените:
SHOP_URL=https://your-new-url.netlify.app

# Перезапустите бота
pm2 restart telegram-bot
```

Подробнее см. [BOT_CONFIG.md](./BOT_CONFIG.md)

---

## Известные проблемы и решения

### 1. Ошибка "Cannot destructure property 'id' of 'Xn(...)' as it is null"

**Проблема:** Ошибка при переходе на страницу товаров.

**Решение:** Используется безопасное обращение к `useTelegramUser()`:
```typescript
const telegramUser = useTelegramUser();
const id = telegramUser?.id; // ✅ Правильно
// const { id } = useTelegramUser(); // ❌ Неправильно
```

### 2. Черный экран при загрузке в Telegram

**Проблема:** Приложение не загружается в Telegram Web App.

**Решение:** 
- Убедитесь, что `telegram.js` загружается синхронно (без `defer`)
- Проверьте правильность chunk splitting в `vite.config.ts`
- Все React-зависимости должны быть в основном бандле

### 3. Персидский календарь в админке

**Проблема:** Календарь отображается на персидском языке.

**Решение:** Используется стандартный `DatePicker` из `antd` с русской локалью (`ru_RU`).

### 4. Текст вводится справа налево

**Проблема:** В полях ввода текст печатается справа налево.

**Решение:** 
- `ConfigProvider` настроен на `direction="ltr"`
- Добавлены стили для принудительного LTR направления

### 5. Кнопки невидимы на темной теме телефона

**Проблема:** Кнопки с прозрачным фоном становятся невидимыми.

**Решение:** Все кнопки имеют непрозрачный серый фон (`#e5e5e5`).

---

## Версия проекта

**Текущая версия:** v0.0.2

### История версий

- **v0.0.2** - Очистка проекта, добавление комментариев, финальная оптимизация
- **v0.0.1** - Первая рабочая версия

---

## Структура проекта

```
telegram-web-app-shop/
├── src/                    # Исходный код
│   ├── components/         # Переиспользуемые компоненты
│   ├── pages/             # Страницы приложения
│   ├── framework/         # API и бизнес-логика
│   ├── layouts/           # Макеты страниц
│   ├── hooks/             # Кастомные хуки
│   └── assets/            # Статические ресурсы
├── public/                 # Публичные файлы
├── scripts/                # Скрипты для автоматизации
├── dist/                   # Собранные файлы (не коммитится)
├── .env                    # Переменные окружения (не коммитится)
├── netlify.toml            # Конфигурация Netlify
├── vite.config.ts          # Конфигурация Vite
└── package.json            # Зависимости проекта
```

---

## Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run prev

# Линтинг
npm run lint:eslint
npm run fix:eslint

# Обновление настроек бота
npm run update-bot
```

---

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Проверьте логи Netlify
3. Убедитесь, что API сервер доступен
4. Проверьте настройки бота в [BOT_CONFIG.md](./BOT_CONFIG.md)
5. См. раздел [Известные проблемы](#известные-проблемы-и-решения)

---

## Лицензия

Проект создан для внутреннего использования.

