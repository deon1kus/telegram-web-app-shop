# 🔧 Обновление конфигурации API для VPS

## ✅ Что было сделано:

1. **Обновлен `netlify.toml`:**
   - Проксирование `/api/*` → `http://194.87.0.193:8000/api/*`
   - Добавлен порт 8000 в URL проксирования

2. **Обновлен `.env.example`:**
   - Добавлены комментарии о работе с Netlify
   - Указан правильный URL для локальной разработки

3. **Коммит отправлен в GitHub:**
   - Netlify автоматически пересоберет проект

## 📋 Как это работает:

### На Netlify (продакшен):
- Запросы к `/api/*` автоматически проксируются на `http://194.87.0.193:8000/api/*`
- Переменная `VITE_API_URL` не нужна (используется относительный путь `/api`)
- Netlify автоматически использует HTTPS для проксирования

### Локальная разработка:
- Установите `VITE_API_URL=http://194.87.0.193:8000` в `.env` файле
- Или используйте `VITE_API_URL=http://localhost:8000` для локального API

## 🔍 Проверка работы:

1. **Дождитесь пересборки Netlify** (обычно 2-5 минут)
2. **Откройте магазин:** https://celadon-starburst-162af0.netlify.app/
3. **Проверьте консоль браузера (F12):**
   - Запросы должны идти на `/api/products`
   - Netlify автоматически проксирует на `194.87.0.193:8000`

## ⚠️ Важно:

- **Для web.telegram.org:** URL должен быть HTTPS (Netlify уже использует HTTPS)
- **Проверьте BotFather:** URL магазина должен быть `https://celadon-starburst-162af0.netlify.app/`

## 🚀 Следующие шаги:

1. Дождитесь завершения сборки на Netlify
2. Проверьте работу магазина
3. Если товары не отображаются, проверьте:
   - Консоль браузера (F12) на наличие ошибок
   - Network tab - какие запросы идут к API
   - Логи Netlify на наличие ошибок сборки

