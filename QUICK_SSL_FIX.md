# Быстрое решение проблемы с SSL

## Проблема

Telegram Web App не открывается на телефоне из-за отсутствия валидного SSL сертификата.

## ⚡ Быстрое решение (15 минут)

### Шаг 1: Получить бесплатный домен

1. Зарегистрируйтесь на https://freenom.com (бесплатные домены .tk, .ml, .ga)
2. Или купите дешевый домен на https://reg.ru (от 99₽/год)

### Шаг 2: Настроить DNS

В настройках домена добавьте A-запись:
- Имя: `@` (или оставьте пустым)
- Значение: `194.87.0.193`
- TTL: 3600

### Шаг 3: Установить Certbot на сервере

Подключитесь к серверу и выполните:

```bash
# Для Ubuntu/Debian
sudo apt update
sudo apt install certbot python3-certbot-nginx -y

# Для CentOS/RHEL
sudo yum install certbot python3-certbot-nginx -y
```

### Шаг 4: Получить сертификат

```bash
# Замените yourdomain.com на ваш домен
sudo certbot --nginx -d yourdomain.com

# Следуйте инструкциям:
# - Введите email
# - Согласитесь с условиями
# - Certbot автоматически настроит Nginx
```

### Шаг 5: Обновить .env

```env
VITE_API_URL=https://yourdomain.com
```

### Шаг 6: Обновить BotFather

1. Откройте @BotFather в Telegram
2. `/mybots` → выберите бота → "Bot Settings" → "Menu Button"
3. Укажите: `https://yourdomain.com`

### Шаг 7: Пересобрать и загрузить

```bash
npm run build
# Загрузите файлы из dist/ на сервер
```

## ✅ Готово!

Теперь приложение должно работать на мобильных устройствах.

## 🔄 Альтернатива: Cloudflare (еще быстрее)

Если не хотите настраивать сертификат на сервере:

1. Зарегистрируйтесь на https://cloudflare.com
2. Добавьте домен
3. Настройте DNS (A-запись на 194.87.0.193)
4. Включите прокси (оранжевое облако)
5. Cloudflare автоматически предоставит SSL

## ⚠️ Важно

- **Без HTTPS приложение не будет работать на мобильных**
- **IP адрес не подходит** - нужен домен
- **Самоподписанные сертификаты не работают** - нужен валидный

