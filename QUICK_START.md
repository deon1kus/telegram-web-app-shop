# Быстрый старт и решение проблем

## ✅ Исправленные проблемы

1. **Ошибка парсинга данных Telegram** - исправлена в `src/hooks/useTelegramUser.ts`
2. **Инициализация Telegram Web App** - добавлен `WebApp.ready()` и `WebApp.expand()` в `src/App.tsx`
3. **Обработка ошибок API** - улучшена в `src/framework/api/utils/api-config.ts`

## 🚀 Быстрый запуск

### 1. Установка зависимостей
```bash
cd D:\WebApp\telegram-web-app-shop-local
npm install
# или
pnpm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:
```env
VITE_API_URL=https://your-api-server.com
```

**Важно:** Замените `https://your-api-server.com` на реальный URL вашего бэкенд API.

### 3. Запуск в режиме разработки
```bash
npm run dev
# или
pnpm dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 4. Сборка для продакшена
```bash
npm run build
# или
pnpm build
```

## 🔍 Диагностика черного экрана

### Шаг 1: Откройте консоль разработчика

**В Telegram:**
1. Откройте приложение в Telegram
2. Нажмите на три точки (⋮) в правом верхнем углу
3. Выберите "Открыть в браузере"
4. В браузере нажмите F12

**Или напрямую в браузере:**
1. Откройте приложение в браузере
2. Нажмите F12

### Шаг 2: Проверьте консоль на ошибки

Ищите красные сообщения об ошибках. Типичные проблемы:

#### Ошибка: "API Error: Network Error"
**Причина:** API недоступен или URL неправильный
**Решение:** 
- Проверьте файл `.env` и убедитесь, что `VITE_API_URL` указан правильно
- Проверьте, что бэкенд сервер запущен и доступен
- Проверьте CORS настройки на сервере

#### Ошибка: "Error parsing Telegram user data"
**Причина:** Приложение не запущено в контексте Telegram Web App
**Решение:**
- Убедитесь, что приложение открывается через Telegram бота
- Проверьте настройки бота в BotFather

#### Ошибка: "Cannot read property 'WebApp' of undefined"
**Причина:** Скрипт `telegram.js` не загрузился
**Решение:**
- Проверьте, что файл `public/telegram.js` существует
- Проверьте консоль на ошибки загрузки скриптов

### Шаг 3: Проверьте данные Telegram

В консоли выполните:
```javascript
// Проверка объекта Telegram
console.log('Telegram:', window.Telegram);

// Проверка данных инициализации
console.log('Init Params:', sessionStorage.getItem('__telegram__initParams'));

// Проверка WebApp
console.log('WebApp:', window.Telegram?.WebApp);
```

**Ожидаемый результат:**
- `window.Telegram` должен быть объектом
- `window.Telegram.WebApp` должен быть объектом с методами
- `sessionStorage.getItem('__telegram__initParams')` должен содержать JSON строку

### Шаг 4: Проверьте Network вкладку

1. Откройте вкладку **Network** в DevTools
2. Обновите страницу (F5)
3. Проверьте запросы к API:
   - Должны быть запросы к `/api/...`
   - Статус должен быть 200 (зеленый) или другой успешный
   - Если статус красный (404, 500, CORS), проверьте настройки API

## 🛠️ Частые проблемы и решения

### Проблема: Черный экран, но нет ошибок в консоли

**Возможные причины:**
1. Приложение не инициализировалось
2. CSS не загрузился
3. React не отрендерился

**Решение:**
1. Проверьте вкладку **Elements** в DevTools - должен быть элемент `<div id="root">`
2. Проверьте, что все CSS файлы загрузились (вкладка Network)
3. Попробуйте очистить кеш браузера (Ctrl+Shift+Delete)

### Проблема: "open in telegram app" сообщение

**Причина:** Приложение не может получить данные пользователя Telegram

**Решение:**
1. Убедитесь, что открываете приложение через Telegram бота
2. Проверьте, что бот правильно настроен в BotFather
3. Проверьте Web App URL в настройках бота

### Проблема: Изображения не загружаются

**Причина:** `VITE_API_URL` не указан или неправильный

**Решение:**
1. Создайте файл `.env` с правильным `VITE_API_URL`
2. Перезапустите dev сервер
3. Для продакшена пересоберите приложение: `npm run build`

## 📝 Чеклист перед деплоем

- [ ] Файл `.env` создан с правильным `VITE_API_URL`
- [ ] Зависимости установлены (`npm install`)
- [ ] Приложение запускается локально без ошибок
- [ ] Консоль браузера не показывает критических ошибок
- [ ] API доступен и отвечает на запросы
- [ ] CORS настроен правильно на сервере
- [ ] Приложение собрано для продакшена (`npm run build`)
- [ ] Файлы из `dist/` загружены на хостинг
- [ ] HTTPS настроен (обязательно для Telegram Web App)
- [ ] Web App URL правильно указан в BotFather

## 🆘 Если ничего не помогает

1. **Очистите кеш:**
   - Браузер: Ctrl+Shift+Delete
   - Telegram: Настройки → Данные и хранилище → Очистить кеш

2. **Переустановите зависимости:**
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. **Проверьте версию Node.js:**
   ```bash
   node --version
   ```
   Должна быть версия 18 или выше

4. **Проверьте логи сервера:**
   - Если используете свой сервер, проверьте логи на наличие ошибок
   - Проверьте доступность API с помощью curl или Postman

## 📞 Дополнительная информация

Подробная информация о решении проблем: см. файл `TROUBLESHOOTING.md`

