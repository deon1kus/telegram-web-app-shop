# Исправление настройки переменной окружения в Netlify

## ⚠️ Важно!

Переменная `VITE_API_URL` была добавлена в `netlify.toml` в секцию `[build.environment]`, но для Vite это **неправильно**!

Переменные в `[build.environment]` доступны только во время сборки, но Vite нужны переменные, которые будут доступны в **runtime** (во время работы приложения).

## ✅ Правильная настройка

### Шаг 1: Удалить из netlify.toml

Откройте `netlify.toml` и **удалите** секцию с переменной окружения:

```toml
# УДАЛИТЬ ЭТО:
[build.environment]
  VITE_API_URL = "http://194.87.0.193"
```

### Шаг 2: Настроить через UI Netlify

1. В Netlify перейдите в ваш проект **webappshop1**
2. Откройте **Site settings** → **Environment variables**
3. Нажмите **"Add variable"**
4. Добавьте:
   - **Key:** `VITE_API_URL`
   - **Value:** `http://194.87.0.193`
   - **Scopes:** Выберите все (Production, Deploy previews, Branch deploys)
5. Нажмите **"Save"**

### Шаг 3: Пересобрать сайт

1. Перейдите в **Deploys**
2. Нажмите **"Trigger deploy"** → **"Deploy site"**
3. Дождитесь завершения деплоя

## ⚠️ Проблема с HTTP

Использование `http://194.87.0.193` (без SSL) может вызвать проблемы:

1. **Mixed Content** - браузеры блокируют HTTP запросы с HTTPS страниц
2. **CORS** - могут быть проблемы с CORS политикой
3. **Безопасность** - данные передаются незашифрованными

## ✅ Решение: Проксирование через Netlify

Лучшее решение - проксировать API запросы через Netlify. Это решит все проблемы!

### Шаг 1: Обновить netlify.toml

Откройте `netlify.toml` и **раскомментируйте** секцию проксирования:

```toml
# Проксирование API запросов на ваш сервер
[[redirects]]
  from = "/api/*"
  to = "http://194.87.0.193/api/:splat"
  status = 200
  force = true
```

### Шаг 2: Удалить переменную VITE_API_URL

1. В Netlify: **Site settings** → **Environment variables**
2. Найдите `VITE_API_URL`
3. Нажмите **"Delete"** или просто **не добавляйте её**

### Шаг 3: Обновить код (опционально)

Код уже настроен правильно - если `VITE_API_URL` не указан, используется относительный путь `/api`, который будет проксироваться через Netlify.

### Шаг 4: Закоммитить и запушить

```bash
git add netlify.toml
git commit -m "Enable API proxying through Netlify"
git push origin main
```

Netlify автоматически пересоберет сайт.

## 🎯 Как это работает

```
Telegram Web App
    ↓ HTTPS
Netlify (Frontend)
    ↓ /api/* запросы проксируются
Netlify Proxy
    ↓ HTTP
Ваш сервер 194.87.0.193 (Backend)
```

**Преимущества:**
- ✅ Все запросы идут через HTTPS
- ✅ Нет проблем с CORS
- ✅ Нет проблем с Mixed Content
- ✅ Не нужно настраивать SSL на сервере

## 📝 Проверка

После настройки:

1. Откройте сайт в браузере
2. Откройте консоль (F12) → вкладка **Network**
3. Выполните какое-то действие в приложении
4. Проверьте запросы к `/api/*` - они должны работать
5. Проверьте, что нет ошибок CORS

## 🔄 Альтернатива: Если нужно использовать переменную

Если всё же хотите использовать переменную окружения (не проксирование):

1. Настройте переменную через UI (см. выше)
2. Убедитесь, что она добавлена во все scopes
3. Пересоберите сайт
4. **Но помните:** могут быть проблемы с HTTP запросами с HTTPS страницы!

## ✅ Рекомендация

**Используйте проксирование через Netlify** - это самое простое и надежное решение!

