# Исправления для Netlify

## 🔧 Исправленные проблемы

### 1. ✅ Порядок редиректов в netlify.toml

**Проблема:** Редирект для SPA (`/* -> /index.html`) был первым, что перехватывало все запросы, включая API.

**Исправление:** 
- API редирект (`/api/*`) теперь ПЕРВЫЙ
- SPA редирект (`/*`) теперь ПОСЛЕДНИЙ

Это критически важно! Netlify обрабатывает редиректы по порядку, и первый совпавший редирект применяется.

### 2. ✅ Отключена проверка Telegram для тестирования

**Проблема:** При открытии в браузере (не в Telegram) приложение показывало сообщение "Пожалуйста, откройте приложение в Telegram".

**Исправление:** Временно отключена проверка для тестирования. В продакшене можно вернуть.

### 3. ✅ Улучшена конфигурация Vite

**Добавлено:**
- Явное указание `base: "/"` для правильной работы на Netlify
- Указание `assetsDir` и `outDir` для ясности

## 📝 Финальная конфигурация netlify.toml

```toml
# Проксирование API запросов - ПЕРВЫЙ (важно!)
[[redirects]]
  from = "/api/*"
  to = "http://194.87.0.193/api/:splat"
  status = 200
  force = true

# SPA редирект - ПОСЛЕДНИЙ (важно!)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

## ✅ Что должно работать теперь

1. **API запросы** - проксируются на ваш сервер
2. **Роутинг** - работает правильно для всех страниц
3. **Статические файлы** - загружаются корректно
4. **Работа в браузере** - можно тестировать без Telegram

## 🔄 После деплоя

Netlify автоматически пересоберет сайт после push в GitHub.

Проверьте:
1. Откройте сайт в браузере
2. Проверьте консоль (F12) на ошибки
3. Проверьте работу API запросов
4. Проверьте роутинг (переходы между страницами)

## 🐛 Если все еще есть проблемы

### Проблема: "404 на всех страницах"

**Решение:**
1. Убедитесь, что `public/_redirects` существует
2. Проверьте порядок редиректов в `netlify.toml`

### Проблема: "API запросы не работают"

**Решение:**
1. Проверьте, что API редирект ПЕРВЫЙ в `netlify.toml`
2. Проверьте, что ваш сервер доступен: `curl http://194.87.0.193/api/`
3. Проверьте логи в Netlify

### Проблема: "Белый экран"

**Решение:**
1. Откройте консоль (F12)
2. Проверьте ошибки JavaScript
3. Проверьте, что все файлы загружаются (вкладка Network)

