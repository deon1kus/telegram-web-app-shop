# ✅ Настройка .env файла завершена

## Что было сделано:

1. ✅ **Создан файл `.env`** в корне проекта
2. ✅ **Добавлен пример конфигурации** с комментариями
3. ✅ **Файл `.env` уже в `.gitignore`** - не будет закоммичен в Git

## 📝 Что нужно сделать вам:

### Шаг 1: Откройте файл `.env`

Файл находится здесь: `D:\WebApp\telegram-web-app-shop-local\.env`

Откройте его в любом текстовом редакторе (Блокнот, VS Code и т.д.)

### Шаг 2: Укажите URL вашего API

Найдите строку:
```
VITE_API_URL=https://your-api-server.com
```

И замените `https://your-api-server.com` на реальный URL вашего бэкенд API.

**Примеры правильных значений:**

```env
# Если API на том же домене
VITE_API_URL=https://yourdomain.com

# Если API на поддомене
VITE_API_URL=https://api.yourdomain.com

# Если API на другом домене
VITE_API_URL=https://backend.example.com

# Для локальной разработки
VITE_API_URL=http://localhost:8000
```

### Шаг 3: Сохраните файл

Сохраните изменения в файле `.env`

### Шаг 4: Перезапустите dev сервер

Если dev сервер уже запущен:

1. Остановите его (нажмите `Ctrl+C` в терминале)
2. Запустите снова: `npm run dev`

## ⚠️ Важно знать:

1. **Если не знаете URL API:**
   - Спросите у разработчика бэкенда
   - Проверьте документацию проекта
   - Или оставьте значение по умолчанию (приложение будет работать, но API запросы будут падать)

2. **Для продакшена:**
   - Обязательно используйте HTTPS
   - Telegram Web App требует HTTPS для работы

3. **Файл .env не коммитится в Git:**
   - Это правильно и безопасно
   - Каждый разработчик создает свой `.env` локально

## 🧪 Как проверить, что всё работает:

1. Запустите приложение: `npm run dev`
2. Откройте в браузере: `http://localhost:3000`
3. Откройте консоль разработчика (F12)
4. Перейдите на вкладку **Network**
5. Попробуйте выполнить какое-то действие в приложении
6. Проверьте запросы - они должны идти на ваш API URL

## 📚 Дополнительная информация:

- Подробная инструкция: см. файл `SETUP_API_URL.md`
- Быстрый старт: см. файл `QUICK_START.md`
- Решение проблем: см. файл `TROUBLESHOOTING.md`

## ✅ Готово!

После настройки `.env` файла вы можете запускать приложение. Все остальные настройки уже выполнены автоматически!

